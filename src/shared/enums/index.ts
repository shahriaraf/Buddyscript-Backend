export enum PostVisibility {
  PUBLIC = 'public',
  PRIVATE = 'private',
}

export enum ReactionTarget {
  POST = 'post',
  COMMENT = 'comment',
}

export enum TokenType {
  ACCESS = 'access',
  REFRESH = 'refresh',
}
